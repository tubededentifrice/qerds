# QERDS Development Environment Configuration
# Copy to .env and customize. Do NOT commit .env (contains secrets).
#
# Usage: cp .env.example .env && nano .env

# --------------------------------------------------------------------------
# PostgreSQL Database
# --------------------------------------------------------------------------
# Password for the qerds database user
POSTGRES_PASSWORD=qerds_dev_password

# --------------------------------------------------------------------------
# MinIO (S3-compatible object storage)
# --------------------------------------------------------------------------
# Root credentials for MinIO admin access
MINIO_ROOT_USER=qerds_minio
MINIO_ROOT_PASSWORD=qerds_minio_secret

# --------------------------------------------------------------------------
# Internal OCI Registry (security-tools profile only)
# --------------------------------------------------------------------------
# Credentials for the internal container registry
# Only needed when running: docker compose --profile security-tools up
QERDS_REGISTRY_USER=qerds
QERDS_REGISTRY_PASSWORD=change_me
