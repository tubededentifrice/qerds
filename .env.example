# Copy to .env and set secrets (do NOT commit .env)
#
# Internal OCI registry credentials (only needed when enabling the security-tools profile):
#   docker compose --profile security-tools up
QERDS_REGISTRY_USER=qerds
QERDS_REGISTRY_PASSWORD=change_me

