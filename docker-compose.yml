# QERDS Development Environment
# Run with: docker compose up

services:
  # UI Mocks Server
  mocks:
    build:
      context: .
      dockerfile: docker/Dockerfile.mocks
    ports:
      - "5000:5000"
    volumes:
      # Mount for live reload during development
      - ./mocks/templates:/app/templates:ro
      - ./mocks/static:/app/static:ro
      - ./mocks/app.py:/app/app.py:ro
    environment:
      - FLASK_DEBUG=1
      - FLASK_ENV=development
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:5000/')"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s

  # Future services (placeholders for full QERDS implementation)
  # Uncomment when implementing backend

  # postgres:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_DB: qerds
  #     POSTGRES_USER: qerds
  #     POSTGRES_PASSWORD: qerds_dev_password
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U qerds"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5

  # minio:
  #   image: minio/minio:latest
  #   command: server /data --console-address ":9001"
  #   environment:
  #     MINIO_ROOT_USER: qerds_minio
  #     MINIO_ROOT_PASSWORD: qerds_minio_secret
  #   volumes:
  #     - minio_data:/data
  #   ports:
  #     - "9000:9000"
  #     - "9001:9001"
  #   healthcheck:
  #     test: ["CMD", "mc", "ready", "local"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5

  # mailpit:
  #   image: axllent/mailpit:latest
  #   ports:
  #     - "8025:8025"  # Web UI
  #     - "1025:1025"  # SMTP
  #   environment:
  #     MP_SMTP_AUTH_ACCEPT_ANY: 1
  #     MP_SMTP_AUTH_ALLOW_INSECURE: 1

# volumes:
#   postgres_data:
#   minio_data:

networks:
  default:
    name: qerds_network
